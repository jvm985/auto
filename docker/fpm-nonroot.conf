; PHP-FPM running as www-data can't open /proc/self/fd/2 (default error_log)
; because that fd was created by dockerd as root. Point to a writable file
; in the storage volume. Worker stdout/stderr still goes to docker via
; catch_workers_output.

[global]
error_log = /var/www/html/storage/logs/php-fpm.log

[www]
catch_workers_output = yes
decorate_workers_output = no
clear_env = no
